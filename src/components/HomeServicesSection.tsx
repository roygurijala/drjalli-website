import { FeaturedServicesGrid } from "@/components/FeaturedServicesGrid";

/**
 * Home: single services showcase (curated grid + directory CTA). Carousel removed to avoid repeating the same cards.
 */
export function HomeServicesSection() {
  return <FeaturedServicesGrid />;
}
